{
  "name": "build-executor",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node --env-file-if-exists=.env server.mjs",
    "server:watch": "node --watch --env-file-if-exists=.env server.mjs"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "lucide": "^0.468.0",
    "ws": "^8.18.0"
  }
}